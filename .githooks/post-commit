#!/bin/sh
# 开发阶段：每次成功提交后，自动推送当前分支到 GitHub。
# 网络或权限暂时不可用时，不影响本地提交；下次提交或手动执行 git push 会补同步。
branch="$(git branch --show-current)"

if [ -n "$branch" ]; then
  # 同步推送，确保提交成功后 GitHub 一定收到；推送失败会明确提示。
  git push origin "$branch" || echo "自动推送未完成，请稍后执行 git push origin $branch"
fi
